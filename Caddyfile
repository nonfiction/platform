# Caddyfile

{

  email dns@nonfiction.ca 
  on_demand_tls {
    interval 5s
    burst 1
  }

  # # This doesn't work yet
  # servers {
  #   listener_wrappers {
  #     proxy_protocol {
  #       timeout 5s
  #       # allow 0.0.0.0\0 
  #     }
  #     tls
  #   }
  # }

}

import /config/*/Caddyfile
