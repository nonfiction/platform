#!/bin/bash
# Domain from swarm with node removed

SWARM=$(docker context inspect --format "{{lower .Endpoints.docker.Host}}" | awk -F @ '{print $2}')
[ -z "$SWARM" ] && SWARM=$(hostname -f)

echo $SWARM | awk -F'.' '{$1=""}1' | xargs | tr ' ' '.'
